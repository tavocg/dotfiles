- manager: apt-get
  depends:
    - apt-get
  remotes:
    - remote: all
      tasks:
        - task: sync
          cmd: apt-get update
          privileged: true
        - task: update-all
          cmd: apt-get full-upgrade -y
          privileged: true
